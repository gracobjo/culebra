# Entorno Python — predicción showroom / Kaggle
#
# Crear y activar (PowerShell):
#   py -3.11 -m venv .venv
#   .\.venv\Scripts\Activate.ps1
#   python -m pip install --upgrade pip
#   pip install -r ml/requirements.txt
#
# Datasets (API Kaggle: https://www.kaggle.com/settings → Create New Token → ~/.kaggle/kaggle.json):
#   mkdir data\kaggle
#   kaggle datasets download -d mmumairkhattak/retail-sales-forecast-dataset-2026 -p data/kaggle/retail-forecast --unzip

pandas>=2.2,<3
numpy>=1.26,<3
scikit-learn>=1.5,<2
matplotlib>=3.8,<4
seaborn>=0.13,<1
jupyter>=1.0,<2
kaggle>=1.6,<2
openpyxl>=3.1,<4
pyarrow>=15,<20
